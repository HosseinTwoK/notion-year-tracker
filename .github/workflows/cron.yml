name: Cron job to trigger Notion update

on:
  schedule:
    - cron: "*/1 * * * *"  # Every minute (GitHub Actions doesn’t support seconds-level granularity)
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
      - name: Send HTTP request to Vercel API route
        run: |
          curl -X POST https://notion-year-tracker-git-main-hosseintwoks-projects.vercel.app/
